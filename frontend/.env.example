# Left unset by default -- see .env for why (LAN/two-phone demo relies on
# the relative-path + vite proxy setup in vite.config.ts). Uncomment only
# for a split-origin setup.
# VITE_API_BASE=http://localhost:8000
# VITE_WS_URL=ws://localhost:8000/ws
